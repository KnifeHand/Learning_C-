If you take a look at the source code, the class defineds and object called Volume. In object oriented programming we can define objects of the same name
but if you have the class file of the same name you will encounter what is called a "Collision".  By separating the classes in different files we can 
give the object of the same names and inherit them later if we need them.